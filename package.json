{
  "name": "saferpet",
  "version": "1.0.0",
  "description": "SaferPet — SaaS de controle de pacotes pré-pagos de banho para petshop (API + painel + portal do cliente)",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node backend/server.js",
    "migrate": "node backend/migrate.js",
    "teste:local": "node scripts/testar-local.mjs",
    "teste:api": "node scripts/testar-api.mjs"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
